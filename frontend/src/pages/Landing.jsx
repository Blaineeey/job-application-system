import { Container, Typography } from '@mui/material';
export default function Landing() {
  return (
    <Container sx={{ mt: 10 }}>
      <Typography variant="h4">Welcome to the Job Application System</Typography>
    </Container>
  );
}